import { TypeWithKey } from "@/models/type-with-key"

export const getValidatorErrorMessage = (validatorName: string) => {
  const config: TypeWithKey<string> = {
    ERR_CANCELED: 'No se que paso hermano'
  }
}